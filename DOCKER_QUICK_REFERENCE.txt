╔════════════════════════════════════════════════════════════════════╗
║                   🐳 SHOWFOLIO DOCKER QUICK START                  ║
╚════════════════════════════════════════════════════════════════════╝

📦 SETUP (First Time Only)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option 1: Automated (Recommended)
  $ ./docker-setup.sh

Option 2: Manual
  $ cp .env.example .env
  $ cp server/.env.example server/.env
  $ cp client/.env.example client/.env


🚀 START SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Production Mode:
  $ docker compose up --build
  $ docker compose up -d              # Background mode

Development Mode (Hot Reload):
  $ docker compose -f docker-compose.dev.yml up --build


🌐 ACCESS POINTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Frontend:        http://localhost:3000
  Strapi Admin:    http://localhost:1337/admin
  Strapi API:      http://localhost:1337/api
  Database:        localhost:5432 (user: showfolio, pass: showfolio)


🛠️ COMMON COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

View Status:
  $ docker compose ps

View Logs:
  $ docker compose logs -f
  $ docker compose logs -f server
  $ docker compose logs -f client

Stop Services:
  $ docker compose down
  $ docker compose down -v            # Also remove volumes

Restart Service:
  $ docker compose restart server

Rebuild:
  $ docker compose build
  $ docker compose build --no-cache

Access Shell:
  $ docker compose exec server sh
  $ docker compose exec client sh
  $ docker compose exec db psql -U showfolio


🔐 SECURITY (IMPORTANT!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  NEVER use default secrets in production!

Generate Secrets:
  $ node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"

Or use docker-setup.sh which generates them automatically.


💾 BACKUP & RESTORE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backup Database:
  $ docker compose exec db pg_dump -U showfolio showfolio > backup.sql

Restore Database:
  $ cat backup.sql | docker compose exec -T db psql -U showfolio showfolio

Backup Uploads:
  $ tar -czf uploads.tar.gz server/public/uploads


🐛 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Port Already in Use:
  $ lsof -i :3000    # Check what's using port
  $ lsof -i :1337
  
  Or change port in docker-compose.yml:
  ports:
    - "3001:3000"

Container Won't Start:
  $ docker compose logs <service>
  $ docker compose restart <service>

Clean Restart:
  $ docker compose down --rmi all --volumes
  $ docker compose up --build

Permission Issues:
  $ chmod -R 755 server/public/uploads


📊 MONITORING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Resource Usage:
  $ docker stats

Health Check:
  $ curl http://localhost:1337/_health
  $ curl http://localhost:3000


📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  DOCKER.md                     - Complete Docker guide
  DOCKER_SETUP_COMPLETE.md      - Setup summary
  README.md                     - Project overview
  .env.example                  - Environment template


📦 WHAT'S INCLUDED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✅ PostgreSQL 15 database
  ✅ Strapi CMS backend (port 1337)
  ✅ Next.js frontend (port 3000)
  ✅ Multi-stage optimized builds
  ✅ Health checks for all services
  ✅ Persistent volumes for data
  ✅ Development mode with hot reload
  ✅ Production-ready configuration


🎯 FIRST TIME SETUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Run: ./docker-setup.sh
2. Run: docker compose up --build
3. Open: http://localhost:1337/admin
4. Create admin account
5. Configure API permissions
6. Open: http://localhost:3000
7. Start building! 🚀


═══════════════════════════════════════════════════════════════════════

Need help? Check DOCKER.md for detailed documentation.

═══════════════════════════════════════════════════════════════════════
