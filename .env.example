# =============================================================================
# SHOWFOLIO - CENTRALIZED ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and update with your values
# This is the ONLY .env file you need - do NOT create client/.env or server/.env
# 
# For production deployment, generate secure random strings:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT
# -----------------------------------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------------------------------
# DOCKER HUB (for building and pushing images)
# -----------------------------------------------------------------------------
# Your Docker Hub username - required for building and pushing images
# Example: DOCKER_USERNAME=johndoe
DOCKER_USERNAME=your_dockerhub_username

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_DB=showfolio
POSTGRES_USER=showfolio
POSTGRES_PASSWORD=showfolio

# For Strapi connection (Docker uses service name 'db', local uses 'localhost')
DATABASE_CLIENT=postgres
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=showfolio
DATABASE_USERNAME=showfolio
DATABASE_PASSWORD=showfolio
DATABASE_SSL=false

# -----------------------------------------------------------------------------
# STRAPI BACKEND CONFIGURATION
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=1337

# Application URLs
APP_URL=http://localhost:1337
ADMIN_URL=http://localhost:1337/admin

# Strapi Security Keys (REQUIRED - Generate new ones for production!)
APP_KEYS=toBeModified1,toBeModified2,toBeModified3,toBeModified4
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# File Upload
MAX_FILE_SIZE=209715200

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:1337

# -----------------------------------------------------------------------------
# NEXT.JS FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
# Public API URL (exposed to browser)
# For local: http://localhost:1337
# For production: https://api.yourdomain.com or http://YOUR_VPS_IP:1337
NEXT_PUBLIC_API_URL=http://localhost:1337

# Server-side API URL (inside Docker network)
API_URL=http://server:1337

# -----------------------------------------------------------------------------
# OPTIONAL: ANALYTICS & MONITORING
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_GA_ID=
# NEXT_PUBLIC_GTM_ID=
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# -----------------------------------------------------------------------------
# OPTIONAL: CLOUD STORAGE (S3, Cloudinary, etc.)
# -----------------------------------------------------------------------------
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_BUCKET=
