# =============================================================================
# ⚠️ DEPRECATED - DO NOT USE THIS FILE
# =============================================================================
# This file is DEPRECATED and kept only for reference.
# 
# ✅ USE: /showfolio/.env (root .env file)
# ❌ DON'T CREATE: server/.env
#
# All environment variables are now centralized in the root .env file.
# Docker Compose will automatically pass the required variables to this service.
#
# If you need to run Strapi outside Docker, copy variables from root .env
# =============================================================================

# These variables are configured in the root .env file:
# - NODE_ENV
# - HOST, PORT
# - APP_URL, ADMIN_URL
# - DATABASE_CLIENT, DATABASE_HOST, DATABASE_PORT, DATABASE_NAME
# - DATABASE_USERNAME, DATABASE_PASSWORD, DATABASE_SSL
# - APP_KEYS, API_TOKEN_SALT, ADMIN_JWT_SECRET
# - TRANSFER_TOKEN_SALT, JWT_SECRET, ENCRYPTION_KEY
# - MAX_FILE_SIZE
# - CORS_ORIGINS
# - AWS credentials (if using cloud storage)
